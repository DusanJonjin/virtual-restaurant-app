export function Guest({ guestNum, orders }) {

    return (
        <div>
            <h5>Guest {guestNum}</h5>
            <button>Order</button>
            <ul><li>guest order</li></ul>
        </div>
    )
}
